---
description: Unity 资源生命周期治理（加载/释放/对象池/场景切换）
globs: Assets/**
alwaysApply: false
---

# 资源生命周期（加载、持有、释放）

- **明确所有权**：任何资源/实例都要回答“谁创建、谁持有、谁释放、何时释放”；避免隐式全局缓存。
- **加载策略**：优先项目既定的可释放加载方案（如 Addressables）；避免把运行时依赖藏在 `Resources` 或静态字段里。
- **释放对称**：凡是 `Load/Instantiate` 对应 `Release/ReturnToPool/Destroy`；场景切换要有统一的收敛点（清理池、取消任务、解除订阅）。
- **对象池要求**：池对象必须有 `OnSpawn/OnDespawn`（或等价）来重置状态；禁止把“未清理状态”带回池中。
- **异步可取消**：加载/延迟任务必须支持取消（`CancellationToken` 或等价），并在 `OnDisable/OnDestroy` 触发取消，避免野回调与泄漏。
- **避免重复加载**：同一 key/地址的资源用共享句柄/引用计数（或集中管理器）；禁止无上限的“加载即忘”。
- **内存目标**：对大纹理/网格/音频/动画等给出预算与降级策略（分辨率、流式、LOD、裁剪）。
